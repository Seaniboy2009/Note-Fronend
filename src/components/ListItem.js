import React from 'react'

const ListItem = ( props ) => {
    const {
        id, 
        title,
        content,
        updated,
        created,
        list,
    } = props
  return (
    <div>ListItem</div>
  )
}

export default ListItem